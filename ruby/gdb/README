Debugging a ruby process using GDB and this module:

move gdbinit in ~/.gdbinit
move ruby in ~/gdb/ruby

once linked to the process in a gbd session, invoke session-ruby

voila, you can now eval "caller" and stuff, realtime on a ruby process
